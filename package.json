{
  "name": "mediastream-to-webm",
  "version": "1.0.1",
  "description": "WebRTC MediaStreams to live webm data streams.",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "standard --fix src/** && zuul --local -- test/*.js",
    "build": "browserify src/index.js -s MediaStreamToWebm -o dist/mediastream-to-webm.js"
  },
  "author": "Thomas Mullen",
  "license": "MIT",
  "devDependencies": {
    "getusermedia": "^2.0.1",
    "standard": "^8.6.0",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "ebml": "^2.2.1",
    "inherits": "^2.0.3",
    "media-recorder-stream": "^2.1.1",
    "mediasource": "^2.1.3",
    "readable-stream": "^2.3.3",
    "webm-cluster-stream": "^1.0.0"
  }
}
